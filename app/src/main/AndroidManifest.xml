<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lindroid.thirdpartylibrariesstudykt">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CatalogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".permissionsdispatcher.PermissionActivity" />
        <activity android:name=".permissionsdispatcher.PermissionDispatchActivity" />
        <activity android:name=".nicespinner.NiceSpinnerActivity" />
        <activity android:name=".xrichtext.XRichTextActivity" />
        <activity android:name=".bgabaseadapter.BaseAdapterActivity" />
        <activity android:name=".materialcalendarview.MaterialCalendarActivity" />
        <activity android:name=".supercalendar.SuperCalendarActivity"></activity>
    </application>

</manifest>